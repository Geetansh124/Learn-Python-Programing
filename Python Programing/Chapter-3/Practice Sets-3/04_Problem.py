name = "I measure the progress of a community by  the degree of progress women have achieved."
print(name.replace("  ","        "))

# **Gyan ki baat
print(name) # Strings are immutable which means that you cannot change them by rnning fnctions of them